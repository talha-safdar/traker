<UserControl x:Class="Traker.Views.SortJobsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:svg="http://sharpvectors.codeplex.com/svgc/"
             xmlns:local="clr-namespace:Traker.Views"
             xmlns:cal="http://caliburnmicro.com"
             mc:Ignorable="d" 
             d:DesignHeight="450" 
             d:DesignWidth="800">
    
    <Border Width="295" VerticalAlignment="Center" Background="{StaticResource SortFilterMainBackground}" CornerRadius="20" BorderThickness="1.5" BorderBrush="#34343A">
        <Border.Effect>
            <DropShadowEffect BlurRadius="15" Opacity="0.3" ShadowDepth="2"/>
        </Border.Effect>
        
        <Grid VerticalAlignment="Center">
            <Grid Margin="5">
                <Grid.RowDefinitions>
                    
                    <!-- order -->
                    <RowDefinition Height="auto" />
                    
                    <!-- spacer -->
                    <RowDefinition Height="auto" />
                    
                    <!-- options -->
                    <RowDefinition Height="auto" />
                </Grid.RowDefinitions>

                <!-- order -->
                <Grid Grid.Row="0">
                    <Border Background="{StaticResource SortFilterMainInnerBackground}" CornerRadius="15" BorderThickness="0,1,0,0" Margin="0,0,0,10">
                        <Border.BorderBrush>
                            <LinearGradientBrush StartPoint="0.5,0" EndPoint="0.5,1" Opacity="0.12">
                                <GradientStop Color="#26FFFFFF" Offset="0" />
                                <GradientStop Color="Transparent" Offset="1" />
                            </LinearGradientBrush>
                        </Border.BorderBrush>
                        <Grid Margin="10,10">
                            <Grid.ColumnDefinitions>
                                
                                <!-- ascending -->
                                <ColumnDefinition Width="*" />
                                
                                <!-- descending -->
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>

                            <!-- ascending -->
                            <Grid Grid.Column="0">
                                <Border Cursor="Hand" Opacity="{Binding OpacityOrderStyle[0]}" cal:Message.Attach="[Event MouseLeftButtonUp] = [Action OrderStyle(0)]" CornerRadius="17" Height="35" Margin="0,0,5,0">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Setter Property="BorderThickness" Value="1.5"/>
                                            <Setter Property="Background" Value="#2A2A2F"/>
                                            <Setter Property="BorderBrush" Value="Transparent"/>
                                            <Style.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Background" Value="#34343A"/>
                                                    <Setter Property="BorderBrush" Value="{StaticResource AccentColour}"/>
                                                </Trigger>

                                                <DataTrigger Binding="{Binding OrderStyleList[0]}" Value="True">
                                                    <Setter Property="BorderBrush" Value="{StaticResource AccentColour}" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <Border.Effect>
                                        <DropShadowEffect BlurRadius="20" Opacity="0.5" ShadowDepth="8" Direction="270"/>
                                    </Border.Effect>
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,3,0,0">
                                        <svg:SvgViewbox                         
                                            RenderOptions.BitmapScalingMode="HighQuality"
                                            SnapsToDevicePixels="True"
                                            Source="/Resources/Media/Images/Icons/Lucide/arrow-down-narrow-wide.svg" Width="15" Height="15"/>
                                        <TextBlock VerticalAlignment="Center" Margin="10,0,0,0" Text="Ascending" FontFamily="pack://Traker:,,,/Resources/Fonts/#Inter" FontWeight="Regular" FontSize="14">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Foreground" Value="#D0D0D6"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsMouseOver, RelativeSource={RelativeSource AncestorType=Border}}" Value="True">
                                                            <Setter Property="Foreground" Value="White"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding OrderStyleList[0]}" Value="True">
                                                            <Setter Property="Foreground" Value="White" />
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </StackPanel>
                                </Border>
                            </Grid>

                            <!-- descending -->
                            <Grid Grid.Column="1">
                                <Border Cursor="Hand" Opacity="{Binding OpacityOrderStyle[1]}" cal:Message.Attach="[Event MouseLeftButtonUp] = [Action OrderStyle(1)]" CornerRadius="17" Height="35" Margin="5,0,0,0">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Setter Property="BorderThickness" Value="1.5"/>
                                            <Setter Property="Background" Value="#2A2A2F"/>
                                            <Setter Property="BorderBrush" Value="Transparent"/>
                                            <Style.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Background" Value="#34343A"/>
                                                    <Setter Property="BorderBrush" Value="{StaticResource AccentColour}"/>
                                                </Trigger>

                                                <DataTrigger Binding="{Binding OrderStyleList[1]}" Value="True">
                                                    <Setter Property="BorderBrush" Value="{StaticResource AccentColour}" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <Border.Effect>
                                        <DropShadowEffect BlurRadius="20" Opacity="0.5" ShadowDepth="8" Direction="270"/>
                                    </Border.Effect>
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,3,0,0">
                                        <svg:SvgViewbox                         
                                            RenderOptions.BitmapScalingMode="HighQuality"
                                            SnapsToDevicePixels="True"
                                            Source="/Resources/Media/Images/Icons/Lucide/arrow-down-wide-narrow.svg" Width="15" Height="15"/>
                                        <TextBlock VerticalAlignment="Center" Margin="10,0,0,0" Text="Descending" FontFamily="pack://Traker:,,,/Resources/Fonts/#Inter" FontWeight="Regular" FontSize="14">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Foreground" Value="#D0D0D6"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsMouseOver, RelativeSource={RelativeSource AncestorType=Border}}" Value="True">
                                                            <Setter Property="Foreground" Value="White"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding OrderStyleList[1]}" Value="True">
                                                            <Setter Property="Foreground" Value="White" />
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </StackPanel>
                                </Border>
                            </Grid>
                        </Grid>
                    </Border>
                </Grid>

                <!-- spacer -->
                <Grid Grid.Row="1" Margin="0,0,0,0" />

                <!-- options -->
                <Grid Grid.Row="2">
                    <Border Background="{StaticResource SortFilterMainInnerBackground}" CornerRadius="15" BorderThickness="1" Height="150" VerticalAlignment="Center">
                        <Border.BorderBrush>
                            <LinearGradientBrush StartPoint="0.5,0" EndPoint="0.5,1" Opacity="0.12">
                                <GradientStop Color="#26FFFFFF" Offset="0" />
                                <GradientStop Color="Transparent" Offset="1" />
                            </LinearGradientBrush>
                        </Border.BorderBrush>

                        <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                            <StackPanel Orientation="Vertical" VerticalAlignment="Center" HorizontalAlignment="Center" Width="250">
                                <!-- client name -->
                                <Grid Height="50">
                                    <Border Cursor="Hand" Opacity="{Binding OpacityOptions[0]}" cal:Message.Attach="[Event MouseLeftButtonUp] = [Action SortOption(0)]" CornerRadius="17" Height="35">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Setter Property="BorderThickness" Value="1.5"/>
                                                <Setter Property="Background" Value="#2A2A2F"/>
                                                <Setter Property="BorderBrush" Value="Transparent"/>
                                                <Style.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter Property="Background" Value="#34343A"/>
                                                        <Setter Property="BorderBrush" Value="{StaticResource AccentColour}"/>
                                                    </Trigger>

                                                    <DataTrigger Binding="{Binding SortOptions[0]}" Value="True">
                                                        <Setter Property="BorderBrush" Value="{StaticResource AccentColour}" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <Border.Effect>
                                            <DropShadowEffect BlurRadius="20" Opacity="0.5" ShadowDepth="8" Direction="270"/>
                                        </Border.Effect>
                                        <TextBlock VerticalAlignment="Center" HorizontalAlignment="Center" Margin="10,0" Text="Client Name" FontFamily="pack://Traker:,,,/Resources/Fonts/#Inter" FontWeight="Regular" FontSize="14">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Foreground" Value="#D0D0D6"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsMouseOver, RelativeSource={RelativeSource AncestorType=Border}}" Value="True">
                                                            <Setter Property="Foreground" Value="White"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding SortOptions[0]}" Value="True">
                                                            <Setter Property="Foreground" Value="White" />
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </Border>
                                </Grid>

                                <!-- job title -->
                                <Grid Height="50">
                                    <Border Cursor="Hand" Opacity="{Binding OpacityOptions[1]}" cal:Message.Attach="[Event MouseLeftButtonUp] = [Action SortOption(1)]" CornerRadius="17" Height="35">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Setter Property="BorderThickness" Value="1.5"/>
                                                <Setter Property="Background" Value="#2A2A2F"/>
                                                <Setter Property="BorderBrush" Value="Transparent"/>
                                                <Style.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter Property="Background" Value="#34343A"/>
                                                        <Setter Property="BorderBrush" Value="{StaticResource AccentColour}"/>
                                                    </Trigger>

                                                    <DataTrigger Binding="{Binding SortOptions[1]}" Value="True">
                                                        <Setter Property="BorderBrush" Value="{StaticResource AccentColour}" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <Border.Effect>
                                            <DropShadowEffect BlurRadius="20" Opacity="0.5" ShadowDepth="8" Direction="270"/>
                                        </Border.Effect>
                                        <TextBlock VerticalAlignment="Center" HorizontalAlignment="Center" Margin="10,0" Text="Job Title" FontFamily="pack://Traker:,,,/Resources/Fonts/#Inter" FontWeight="Regular" FontSize="14">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Foreground" Value="#D0D0D6"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsMouseOver, RelativeSource={RelativeSource AncestorType=Border}}" Value="True">
                                                            <Setter Property="Foreground" Value="White"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding SortOptions[1]}" Value="True">
                                                            <Setter Property="Foreground" Value="White" />
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </Border>
                                </Grid>

                                <!-- job status -->
                                <Grid Height="50">
                                    <Border Cursor="Hand" Opacity="{Binding OpacityOptions[2]}" cal:Message.Attach="[Event MouseLeftButtonUp] = [Action SortOption(2)]" CornerRadius="17" Height="35">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Setter Property="BorderThickness" Value="1.5"/>
                                                <Setter Property="Background" Value="#2A2A2F"/>
                                                <Setter Property="BorderBrush" Value="Transparent"/>
                                                <Style.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter Property="Background" Value="#34343A"/>
                                                        <Setter Property="BorderBrush" Value="{StaticResource AccentColour}"/>
                                                    </Trigger>

                                                    <DataTrigger Binding="{Binding SortOptions[2]}" Value="True">
                                                        <Setter Property="BorderBrush" Value="{StaticResource AccentColour}" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <Border.Effect>
                                            <DropShadowEffect BlurRadius="20" Opacity="0.5" ShadowDepth="8" Direction="270"/>
                                        </Border.Effect>
                                        <TextBlock VerticalAlignment="Center" HorizontalAlignment="Center" Margin="10,0" Text="Job Status" FontFamily="pack://Traker:,,,/Resources/Fonts/#Inter" FontWeight="Regular" FontSize="14">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Foreground" Value="#D0D0D6"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsMouseOver, RelativeSource={RelativeSource AncestorType=Border}}" Value="True">
                                                            <Setter Property="Foreground" Value="White"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding SortOptions[2]}" Value="True">
                                                            <Setter Property="Foreground" Value="White" />
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </Border>
                                </Grid>

                                <!-- job Price -->
                                <Grid Height="50">
                                    <Border Cursor="Hand" Opacity="{Binding OpacityOptions[3]}" cal:Message.Attach="[Event MouseLeftButtonUp] = [Action SortOption(3)]" CornerRadius="17" Height="35">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Setter Property="BorderThickness" Value="1.5"/>
                                                <Setter Property="Background" Value="#2A2A2F"/>
                                                <Setter Property="BorderBrush" Value="Transparent"/>
                                                <Style.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter Property="Background" Value="#34343A"/>
                                                        <Setter Property="BorderBrush" Value="{StaticResource AccentColour}"/>
                                                    </Trigger>

                                                    <DataTrigger Binding="{Binding SortOptions[3]}" Value="True">
                                                        <Setter Property="BorderBrush" Value="{StaticResource AccentColour}" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <Border.Effect>
                                            <DropShadowEffect BlurRadius="20" Opacity="0.5" ShadowDepth="8" Direction="270"/>
                                        </Border.Effect>
                                        <TextBlock VerticalAlignment="Center" HorizontalAlignment="Center" Margin="10,0" Text="Job Price" FontFamily="pack://Traker:,,,/Resources/Fonts/#Inter" FontWeight="Regular" FontSize="14">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Foreground" Value="#D0D0D6"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsMouseOver, RelativeSource={RelativeSource AncestorType=Border}}" Value="True">
                                                            <Setter Property="Foreground" Value="White"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding SortOptions[3]}" Value="True">
                                                            <Setter Property="Foreground" Value="White" />
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </Border>
                                </Grid>

                                <!-- Due Date -->
                                <Grid Height="50">
                                    <Border Cursor="Hand" Opacity="{Binding OpacityOptions[4]}" cal:Message.Attach="[Event MouseLeftButtonUp] = [Action SortOption(4)]" CornerRadius="17" Height="35">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Setter Property="BorderThickness" Value="1.5"/>
                                                <Setter Property="Background" Value="#2A2A2F"/>
                                                <Setter Property="BorderBrush" Value="Transparent"/>
                                                <Style.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter Property="Background" Value="#34343A"/>
                                                        <Setter Property="BorderBrush" Value="{StaticResource AccentColour}"/>
                                                    </Trigger>

                                                    <DataTrigger Binding="{Binding SortOptions[4]}" Value="True">
                                                        <Setter Property="BorderBrush" Value="{StaticResource AccentColour}" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <Border.Effect>
                                            <DropShadowEffect BlurRadius="20" Opacity="0.5" ShadowDepth="8" Direction="270"/>
                                        </Border.Effect>
                                        <TextBlock VerticalAlignment="Center" HorizontalAlignment="Center" Margin="10,0" Text="Due Date" FontFamily="pack://Traker:,,,/Resources/Fonts/#Inter" FontWeight="Regular" FontSize="14">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Foreground" Value="#D0D0D6"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsMouseOver, RelativeSource={RelativeSource AncestorType=Border}}" Value="True">
                                                            <Setter Property="Foreground" Value="White"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding SortOptions[4]}" Value="True">
                                                            <Setter Property="Foreground" Value="White" />
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </Border>
                                </Grid>

                                <!-- created Date -->
                                <Grid Height="50">
                                    <Border Cursor="Hand" Opacity="{Binding OpacityOptions[5]}" cal:Message.Attach="[Event MouseLeftButtonUp] = [Action SortOption(5)]" CornerRadius="17" Height="35">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Setter Property="BorderThickness" Value="1.5"/>
                                                <Setter Property="Background" Value="#2A2A2F"/>
                                                <Setter Property="BorderBrush" Value="Transparent"/>
                                                <Style.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter Property="Background" Value="#34343A"/>
                                                        <Setter Property="BorderBrush" Value="{StaticResource AccentColour}"/>
                                                    </Trigger>

                                                    <DataTrigger Binding="{Binding SortOptions[5]}" Value="True">
                                                        <Setter Property="BorderBrush" Value="{StaticResource AccentColour}" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <Border.Effect>
                                            <DropShadowEffect BlurRadius="20" Opacity="0.5" ShadowDepth="8" Direction="270"/>
                                        </Border.Effect>
                                        <TextBlock VerticalAlignment="Center" HorizontalAlignment="Center" Margin="10,0" Text="Created Date" FontFamily="pack://Traker:,,,/Resources/Fonts/#Inter" FontWeight="Regular" FontSize="14">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Foreground" Value="#D0D0D6"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsMouseOver, RelativeSource={RelativeSource AncestorType=Border}}" Value="True">
                                                            <Setter Property="Foreground" Value="White"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding SortOptions[5]}" Value="True">
                                                            <Setter Property="Foreground" Value="White" />
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </Border>
                                </Grid>

                                <!-- business type -->
                                <Grid Height="50">
                                    <Border Cursor="Hand" Opacity="{Binding OpacityOptions[6]}" cal:Message.Attach="[Event MouseLeftButtonUp] = [Action SortOption(6)]" CornerRadius="17" Height="35">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Setter Property="BorderThickness" Value="1.5"/>
                                                <Setter Property="Background" Value="#2A2A2F"/>
                                                <Setter Property="BorderBrush" Value="Transparent"/>
                                                <Style.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter Property="Background" Value="#34343A"/>
                                                        <Setter Property="BorderBrush" Value="{StaticResource AccentColour}"/>
                                                    </Trigger>

                                                    <DataTrigger Binding="{Binding SortOptions[6]}" Value="True">
                                                        <Setter Property="BorderBrush" Value="{StaticResource AccentColour}" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <Border.Effect>
                                            <DropShadowEffect BlurRadius="20" Opacity="0.5" ShadowDepth="8" Direction="270"/>
                                        </Border.Effect>
                                        <TextBlock VerticalAlignment="Center" HorizontalAlignment="Center" Margin="10,0" Text="Business Type" FontFamily="pack://Traker:,,,/Resources/Fonts/#Inter" FontWeight="Regular" FontSize="14">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Foreground" Value="#D0D0D6"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsMouseOver, RelativeSource={RelativeSource AncestorType=Border}}" Value="True">
                                                            <Setter Property="Foreground" Value="White"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding SortOptions[6]}" Value="True">
                                                            <Setter Property="Foreground" Value="White" />
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </Border>
                                </Grid>
                            </StackPanel>
                        </ScrollViewer>
                    </Border>
                </Grid>
            </Grid>
        </Grid>
    </Border>
</UserControl>