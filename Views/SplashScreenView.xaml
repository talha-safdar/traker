<Window x:Class="Traker.Views.SplashScreenView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:svg="http://sharpvectors.codeplex.com/svgc/"
        xmlns:local="clr-namespace:Traker.Views"
        mc:Ignorable="d"
        WindowStyle="None"
        ResizeMode="NoResize"
        AllowsTransparency="True"
        WindowStartupLocation="CenterScreen"
        ShowInTaskbar="False"
        Topmost="True"
        Background="Transparent" Width="450" Height="300">
    <Border Margin="10" CornerRadius="25">
        <Border.Background>
            <LinearGradientBrush x:Name="AnimatedGradient" StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#1A1A1A" Offset="0"/>
                <GradientStop Color="#1E1E1E" Offset="1"/>
                <LinearGradientBrush.RelativeTransform>
                    <TranslateTransform x:Name="GradientMove"/>
                </LinearGradientBrush.RelativeTransform>
            </LinearGradientBrush>
            </Border.Background>
            <Border.Triggers>
                <EventTrigger RoutedEvent="Loaded">
                    <BeginStoryboard>
                        <Storyboard RepeatBehavior="Forever" AutoReverse="True">
                            <DoubleAnimation
                        Storyboard.TargetName="GradientMove"
                        Storyboard.TargetProperty="X"
                        From="-0.2"
                        To="0.2"
                        Duration="0:0:4"/>

                            <DoubleAnimation
                        Storyboard.TargetName="GradientMove"
                        Storyboard.TargetProperty="Y"
                        From="-0.2"
                        To="0.2"
                        Duration="0:0:4"/>

                        </Storyboard>
                    </BeginStoryboard>
                </EventTrigger>
            </Border.Triggers>
            <Border.Effect>
            <DropShadowEffect BlurRadius="20" ShadowDepth="0" Opacity="0.12" Color="white" />
        </Border.Effect>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*" /> <!-- logo name -->
                <RowDefinition Height="*" /> <!-- text -->
            </Grid.RowDefinitions>
            
            <!-- logo name -->
            <Grid Grid.RowSpan="3">
                <TextBlock VerticalAlignment="Center" HorizontalAlignment="Center" Text="TRAKER" FontFamily="pack://Traker:,,,/Resources/Fonts/#Montserrat" FontWeight="ExtraBlack" Foreground="#E0E0E0" FontSize="36">
                    <TextBlock.Effect>
                        <DropShadowEffect
                          ShadowDepth="0"
                          Color="#E0E0E0"
                          Opacity="1"/>
                    </TextBlock.Effect>

                    <TextBlock.Triggers>
                        <EventTrigger RoutedEvent="Loaded">
                            <BeginStoryboard>
                                <Storyboard RepeatBehavior="Forever"
                            AutoReverse="True">

                                    <DoubleAnimation
                                        Storyboard.TargetProperty="Effect.BlurRadius"
                                        From="5"
                                        To="10"
                                        Duration="0:0:1"/>

                                </Storyboard>
                            </BeginStoryboard>
                        </EventTrigger>
                    </TextBlock.Triggers>
                </TextBlock>
            </Grid>

            <!-- text -->
            <Grid Grid.Row="1">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                    <TextBlock VerticalAlignment="Center" HorizontalAlignment="Center" Text="{Binding State.SplashText}" FontFamily="pack://Traker:,,,/Resources/Fonts/#Inter" FontWeight="Regular" Foreground="#888888" FontSize="18" />
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,0">
                        <TextBlock x:Name="Dot1" Text="." FontFamily="pack://Traker:,,,/Resources/Fonts/#Inter" FontWeight="Regular" Foreground="#888888" FontSize="18" />
                        <TextBlock x:Name="Dot2" Text="." FontFamily="pack://Traker:,,,/Resources/Fonts/#Inter" FontWeight="Regular" Foreground="#888888" FontSize="18" />
                        <TextBlock x:Name="Dot3" Text="." FontFamily="pack://Traker:,,,/Resources/Fonts/#Inter" FontWeight="Regular" Foreground="#888888" FontSize="18" />
                    </StackPanel>

                    <StackPanel.Triggers>
                        <EventTrigger RoutedEvent="StackPanel.Loaded">
                            <BeginStoryboard>
                                <Storyboard RepeatBehavior="Forever">

                                    <DoubleAnimationUsingKeyFrames Storyboard.TargetName="Dot1" Storyboard.TargetProperty="Opacity">
                                        <DiscreteDoubleKeyFrame KeyTime="0:0:0" Value="0.0"/>
                                        <DiscreteDoubleKeyFrame KeyTime="0:0:1" Value="1.0"/>
                                        <DiscreteDoubleKeyFrame KeyTime="0:0:3.5" Value="0.0"/>
                                    </DoubleAnimationUsingKeyFrames>

                                    <DoubleAnimationUsingKeyFrames Storyboard.TargetName="Dot2" Storyboard.TargetProperty="Opacity">
                                        <DiscreteDoubleKeyFrame KeyTime="0:0:0" Value="0.0"/>
                                        <DiscreteDoubleKeyFrame KeyTime="0:0:2" Value="1.0"/>
                                        <DiscreteDoubleKeyFrame KeyTime="0:0:3.5" Value="0.0"/>
                                    </DoubleAnimationUsingKeyFrames>

                                    <DoubleAnimationUsingKeyFrames Storyboard.TargetName="Dot3" Storyboard.TargetProperty="Opacity">
                                        <DiscreteDoubleKeyFrame KeyTime="0:0:0" Value="0.0"/>
                                        <DiscreteDoubleKeyFrame KeyTime="0:0:3" Value="1.0"/>
                                        <DiscreteDoubleKeyFrame KeyTime="0:0:3.5" Value="0.0"/>
                                    </DoubleAnimationUsingKeyFrames>
                                </Storyboard>
                            </BeginStoryboard>
                        </EventTrigger>
                    </StackPanel.Triggers>
                </StackPanel>
            </Grid>
        </Grid>
    </Border>
</Window>
