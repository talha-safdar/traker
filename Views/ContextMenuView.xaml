<UserControl x:Class="Traker.Views.ContextMenuView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:cal="http://caliburnmicro.com"
             xmlns:local="clr-namespace:Traker.Views"
             mc:Ignorable="d" 
             d:DesignHeight="300" 
             d:DesignWidth="250">
    <Border Background="#1E1E1E" CornerRadius="20" BorderBrush="#3F3F46" BorderThickness="1" Width="310" Height="335">
        <Grid Margin="10" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <Grid.RowDefinitions>
                <RowDefinition Height="0.4*" /> <!-- stauts -->
                <RowDefinition Height="*" /> <!-- main buttons -->
                <RowDefinition Height="0.15*" /> <!-- spacer -->
                <RowDefinition Height="0.4*" /> <!-- delete -->
            </Grid.RowDefinitions>

            <!-- status -->
            <Grid Grid.Row="0">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Center">
                    <Border cal:Message.Attach="[Event MouseLeftButtonUp] = [Action SetStatus('Done')]" Background="#1B3320" CornerRadius="10" Height="35" Margin="0,0,30,0" Width="110" BorderThickness="1" BorderBrush="#00FF00">
                        <TextBlock VerticalAlignment="Center" HorizontalAlignment="Center" Margin="10,0" Text="Done" FontFamily="pack://Traker:,,,/Resources/Fonts/#Inter" FontWeight="Regular" Foreground="#4CAF50" FontSize="14" />
                    </Border>
                    <Border cal:Message.Attach="[Event MouseLeftButtonUp] = [Action SetStatus('Active')]" Background="#332B1B" CornerRadius="10" Height="35" Margin="0,0,0,0" Width="110" BorderThickness="1" BorderBrush="#FFAA00">
                        <TextBlock VerticalAlignment="Center" HorizontalAlignment="Center" Margin="10,0" Text="Active" FontFamily="pack://Traker:,,,/Resources/Fonts/#Inter" FontWeight="Regular" Foreground="#FFAA00" FontSize="14" />
                    </Border>
                </StackPanel>
            </Grid>

            <!-- main buttons -->
            <Grid Grid.Row="1">
                <StackPanel Orientation="Vertical" VerticalAlignment="Center" HorizontalAlignment="Center">
                    <Border cal:Message.Attach="[Event MouseLeftButtonUp] = [Action OpenFolder()]" Background="#2D2D30" CornerRadius="17" Height="35" Margin="0,0,0,15" Width="250" BorderThickness="2" BorderBrush="#333333">
                        <TextBlock VerticalAlignment="Center" HorizontalAlignment="Center" Margin="10,0" Text="Open Folder" FontFamily="pack://Traker:,,,/Resources/Fonts/#Inter" FontWeight="Regular" Foreground="#E4E4E7" FontSize="14" />
                    </Border>
                    <Border cal:Message.Attach="[Event MouseLeftButtonUp] = [Action CreateInvoice()]" Background="#2D2D30" CornerRadius="17" Height="35" Margin="0,0,0,15" Width="250" BorderThickness="2" BorderBrush="#333333">
                        <TextBlock VerticalAlignment="Center" HorizontalAlignment="Center" Margin="10,0" Text="Create Invoice" FontFamily="pack://Traker:,,,/Resources/Fonts/#Inter" FontWeight="Regular" Foreground="#E4E4E7" FontSize="14" />
                    </Border>
                    <Border cal:Message.Attach="[Event MouseLeftButtonUp] = [Action EditClient()]" Background="#2D2D30" CornerRadius="17" Height="35" Margin="0,0,0,5" Width="250" BorderThickness="2" BorderBrush="#333333">
                        <TextBlock VerticalAlignment="Center" HorizontalAlignment="Center" Margin="10,0" Text="Edit Client" FontFamily="pack://Traker:,,,/Resources/Fonts/#Inter" FontWeight="Regular" Foreground="#E4E4E7" FontSize="14" />
                    </Border>
                </StackPanel>
            </Grid>
            <Grid Grid.Row="2" Margin="0,10">
                <Rectangle Height="1" HorizontalAlignment="Stretch" VerticalAlignment="Center" Margin="20,0">
                    <Rectangle.Fill>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                            <GradientStop Color="Transparent" Offset="0" />
                            <GradientStop Color="White" Offset="0.5" />
                            <GradientStop Color="Transparent" Offset="1" />
                        </LinearGradientBrush>
                    </Rectangle.Fill>
                </Rectangle>
            </Grid>

            <!-- edit / delete -->
            <Grid Grid.Row="3">
                <Border cal:Message.Attach="[Event MouseLeftButtonUp] = [Action DeleteRow()]" Background="Transparent" CornerRadius="17" Height="35" Margin="0,0,0,5" Width="250" BorderThickness="2" BorderBrush="#F87171">
                    <TextBlock VerticalAlignment="Center" HorizontalAlignment="Center" Margin="10,0" Text="Delete Job" FontFamily="pack://Traker:,,,/Resources/Fonts/#Inter" FontWeight="Regular" Foreground="#F87171" FontSize="14" />
                </Border>
            </Grid>
        </Grid>
        <Border.Effect>
            <DropShadowEffect BlurRadius="15" Opacity="0.3" ShadowDepth="2"/>
        </Border.Effect>
    </Border>
</UserControl>
