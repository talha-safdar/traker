<UserControl x:Class="Traker.Views.FilterJobsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:svg="http://sharpvectors.codeplex.com/svgc/"
             xmlns:cal="http://caliburnmicro.com"
             xmlns:local="clr-namespace:Traker.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Border Width="295" VerticalAlignment="Center" Background="{StaticResource SortFilterMainBackground}" CornerRadius="16" BorderThickness="1.5" BorderBrush="#34343A">
        <Border.Effect>
            <DropShadowEffect BlurRadius="15" Opacity="0.3" ShadowDepth="2"/>
        </Border.Effect>
        <Grid VerticalAlignment="Center">
            <Grid Margin="5">
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto" />                    <!-- stauts -->
                    <RowDefinition Height="auto" />                    <!-- client type -->
                </Grid.RowDefinitions>

                <!-- status -->
                <Grid Grid.Row="0">
                    <Border Background="{StaticResource SortFilterMainInnerBackground}" CornerRadius="11" BorderThickness="0,1,0,0" Margin="0,0,0,10">
                        <Border.BorderBrush>
                            <LinearGradientBrush StartPoint="0.5,0" EndPoint="0.5,1" Opacity="0.12">
                                <GradientStop Color="#26FFFFFF" Offset="0" />
                                <GradientStop Color="Transparent" Offset="1" />
                            </LinearGradientBrush>
                        </Border.BorderBrush>

                        <Grid Margin="10,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="0.1*" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>

                            <Grid.RowDefinitions>
                                <RowDefinition Height="*" />
                                <RowDefinition Height="*" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>

                            <!-- new -->
                            <Grid Grid.Column="0" Grid.Row="0">
                                <Border Cursor="Hand" Opacity="{Binding OpacityStatus[0]}" cal:Message.Attach="[Event MouseLeftButtonUp] = [Action FilterStatusOption(0)]" CornerRadius="15" Height="30" Margin="0,0,0,5">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Setter Property="BorderThickness" Value="1.5"/>
                                            <Setter Property="Background" Value="#2A2A2F"/>
                                            <Setter Property="BorderBrush" Value="Transparent"/>
                                            <Style.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Background" Value="#34343A"/>
                                                    <Setter Property="BorderBrush" Value="{StaticResource AccentColour}"/>
                                                </Trigger>

                                                <DataTrigger Binding="{Binding OptionsStatus[0]}" Value="True">
                                                    <Setter Property="BorderBrush" Value="{StaticResource AccentColour}" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <Border.Effect>
                                        <DropShadowEffect BlurRadius="20" Opacity="0.5" ShadowDepth="8" Direction="270"/>
                                    </Border.Effect>
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="5,3,0,0">
                                        <Grid Width="90">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="auto" /> <!-- dot -->
                                                <ColumnDefinition Width="*" /> <!-- text -->
                                            </Grid.ColumnDefinitions>
                                            
                                            <!-- dot -->
                                            <Grid Grid.Column="0">
                                                <Border Background="{StaticResource JobStatusNew}" Height="10" Width="10" CornerRadius="20" />
                                            </Grid>                                            

                                            <!-- text -->
                                            <Grid Grid.Column="2">
                                                <TextBlock VerticalAlignment="Center" HorizontalAlignment="Center" Text="New" FontFamily="pack://Traker:,,,/Resources/Fonts/#Inter" FontWeight="Regular" FontSize="14">
                                                    <TextBlock.Style>
                                                        <Style TargetType="TextBlock">
                                                            <Setter Property="Foreground" Value="#D0D0D6"/>
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding IsMouseOver, RelativeSource={RelativeSource AncestorType=Border}}" Value="True">
                                                                    <Setter Property="Foreground" Value="White"/>
                                                                </DataTrigger>
                                                                <DataTrigger Binding="{Binding OptionsStatus[0]}" Value="True">
                                                                    <Setter Property="Foreground" Value="White" />
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </TextBlock.Style>
                                                </TextBlock>
                                            </Grid>
                                        </Grid>
                                    </StackPanel>
                                </Border>
                            </Grid>

                            <!-- active -->
                            <Grid Grid.Column="2" Grid.Row="0">
                                <Border Cursor="Hand" Opacity="{Binding OpacityStatus[1]}" cal:Message.Attach="[Event MouseLeftButtonUp] = [Action FilterStatusOption(1)]" CornerRadius="15" Height="30" Margin="0,0,0,5">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Setter Property="BorderThickness" Value="1.5"/>
                                            <Setter Property="Background" Value="#2A2A2F"/>
                                            <Setter Property="BorderBrush" Value="Transparent"/>
                                            <Style.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Background" Value="#34343A"/>
                                                    <Setter Property="BorderBrush" Value="{StaticResource AccentColour}"/>
                                                </Trigger>

                                                <DataTrigger Binding="{Binding OptionsStatus[1]}" Value="True">
                                                    <Setter Property="BorderBrush" Value="{StaticResource AccentColour}" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <Border.Effect>
                                        <DropShadowEffect BlurRadius="20" Opacity="0.5" ShadowDepth="8" Direction="270"/>
                                    </Border.Effect>
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="5,3,0,0">
                                        <Grid Width="90">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="auto" />
                                                <!-- dot -->
                                                <ColumnDefinition Width="*" />
                                                <!-- text -->
                                            </Grid.ColumnDefinitions>

                                            <!-- dot -->
                                            <Grid Grid.Column="0">
                                                <Border Background="{StaticResource JobStatusActive}" Height="10" Width="10" CornerRadius="20" />
                                            </Grid>

                                            <!-- text -->
                                            <Grid Grid.Column="2">
                                                <TextBlock VerticalAlignment="Center" HorizontalAlignment="Center" Text="Active" FontFamily="pack://Traker:,,,/Resources/Fonts/#Inter" FontWeight="Regular" FontSize="14">
                                                    <TextBlock.Style>
                                                        <Style TargetType="TextBlock">
                                                            <Setter Property="Foreground" Value="#D0D0D6"/>
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding IsMouseOver, RelativeSource={RelativeSource AncestorType=Border}}" Value="True">
                                                                    <Setter Property="Foreground" Value="White"/>
                                                                </DataTrigger>
                                                                <DataTrigger Binding="{Binding OptionsStatus[0]}" Value="True">
                                                                    <Setter Property="Foreground" Value="White" />
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </TextBlock.Style>
                                                </TextBlock>
                                            </Grid>
                                        </Grid>
                                    </StackPanel>
                                </Border>
                            </Grid>

                            <!-- done -->
                            <Grid Grid.Column="0" Grid.Row="1">
                                <Border Cursor="Hand" Opacity="{Binding OpacityStatus[2]}" cal:Message.Attach="[Event MouseLeftButtonUp] = [Action FilterStatusOption(2)]" CornerRadius="15" Height="30" Margin="0,0,0,5">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Setter Property="BorderThickness" Value="1.5"/>
                                            <Setter Property="Background" Value="#2A2A2F"/>
                                            <Setter Property="BorderBrush" Value="Transparent"/>
                                            <Style.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Background" Value="#34343A"/>
                                                    <Setter Property="BorderBrush" Value="{StaticResource AccentColour}"/>
                                                </Trigger>

                                                <DataTrigger Binding="{Binding OptionsStatus[2]}" Value="True">
                                                    <Setter Property="BorderBrush" Value="{StaticResource AccentColour}" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <Border.Effect>
                                        <DropShadowEffect BlurRadius="20" Opacity="0.5" ShadowDepth="8" Direction="270"/>
                                    </Border.Effect>
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="5,3,0,0">
                                        <Grid Width="90">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="auto" />
                                                <!-- dot -->
                                                <ColumnDefinition Width="*" />
                                                <!-- text -->
                                            </Grid.ColumnDefinitions>

                                            <!-- dot -->
                                            <Grid Grid.Column="0">
                                                <Border Background="{StaticResource JobStatusDone}" Height="10" Width="10" CornerRadius="20" />
                                            </Grid>

                                            <!-- text -->
                                            <Grid Grid.Column="2">
                                                <TextBlock VerticalAlignment="Center" HorizontalAlignment="Center" Text="Done" FontFamily="pack://Traker:,,,/Resources/Fonts/#Inter" FontWeight="Regular" FontSize="14">
                                                    <TextBlock.Style>
                                                        <Style TargetType="TextBlock">
                                                            <Setter Property="Foreground" Value="#D0D0D6"/>
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding IsMouseOver, RelativeSource={RelativeSource AncestorType=Border}}" Value="True">
                                                                    <Setter Property="Foreground" Value="White"/>
                                                                </DataTrigger>
                                                                <DataTrigger Binding="{Binding OptionsStatus[0]}" Value="True">
                                                                    <Setter Property="Foreground" Value="White" />
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </TextBlock.Style>
                                                </TextBlock>
                                            </Grid>
                                        </Grid>
                                    </StackPanel>
                                </Border>
                            </Grid>

                            <!-- invoiced -->
                            <Grid Grid.Column="2" Grid.Row="1">
                                <Border Cursor="Hand" Opacity="{Binding OpacityStatus[3]}" cal:Message.Attach="[Event MouseLeftButtonUp] = [Action FilterStatusOption(3)]" CornerRadius="15" Height="30" Margin="0,0,0,5">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Setter Property="BorderThickness" Value="1.5"/>
                                            <Setter Property="Background" Value="#2A2A2F"/>
                                            <Setter Property="BorderBrush" Value="Transparent"/>
                                            <Style.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Background" Value="#34343A"/>
                                                    <Setter Property="BorderBrush" Value="{StaticResource AccentColour}"/>
                                                </Trigger>

                                                <DataTrigger Binding="{Binding OptionsStatus[3]}" Value="True">
                                                    <Setter Property="BorderBrush" Value="{StaticResource AccentColour}" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <Border.Effect>
                                        <DropShadowEffect BlurRadius="20" Opacity="0.5" ShadowDepth="8" Direction="270"/>
                                    </Border.Effect>
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="5,3,0,0">
                                        <Grid Width="90">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="auto" />
                                                <!-- dot -->
                                                <ColumnDefinition Width="*" />
                                                <!-- text -->
                                            </Grid.ColumnDefinitions>

                                            <!-- dot -->
                                            <Grid Grid.Column="0">
                                                <Border Background="{StaticResource JobStatusInvoiced}" Height="10" Width="10" CornerRadius="20" />
                                            </Grid>

                                            <!-- text -->
                                            <Grid Grid.Column="2">
                                                <TextBlock VerticalAlignment="Center" HorizontalAlignment="Center" Text="Invoiced" FontFamily="pack://Traker:,,,/Resources/Fonts/#Inter" FontWeight="Regular" FontSize="14">
                                                    <TextBlock.Style>
                                                        <Style TargetType="TextBlock">
                                                            <Setter Property="Foreground" Value="#D0D0D6"/>
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding IsMouseOver, RelativeSource={RelativeSource AncestorType=Border}}" Value="True">
                                                                    <Setter Property="Foreground" Value="White"/>
                                                                </DataTrigger>
                                                                <DataTrigger Binding="{Binding OptionsStatus[0]}" Value="True">
                                                                    <Setter Property="Foreground" Value="White" />
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </TextBlock.Style>
                                                </TextBlock>
                                            </Grid>
                                        </Grid>
                                    </StackPanel>
                                </Border>
                            </Grid>

                            <!-- overdue -->
                            <Grid Grid.Column="0" Grid.Row="2">
                                <Border Cursor="Hand" Opacity="{Binding OpacityStatus[4]}" cal:Message.Attach="[Event MouseLeftButtonUp] = [Action FilterStatusOption(4)]" CornerRadius="15" Height="30">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Setter Property="BorderThickness" Value="1.5"/>
                                            <Setter Property="Background" Value="#2A2A2F"/>
                                            <Setter Property="BorderBrush" Value="Transparent"/>
                                            <Style.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Background" Value="#34343A"/>
                                                    <Setter Property="BorderBrush" Value="{StaticResource AccentColour}"/>
                                                </Trigger>

                                                <DataTrigger Binding="{Binding OptionsStatus[4]}" Value="True">
                                                    <Setter Property="BorderBrush" Value="{StaticResource AccentColour}" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <Border.Effect>
                                        <DropShadowEffect BlurRadius="20" Opacity="0.5" ShadowDepth="8" Direction="270"/>
                                    </Border.Effect>
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="5,3,0,0">
                                        <Grid Width="90">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="auto" />
                                                <!-- dot -->
                                                <ColumnDefinition Width="*" />
                                                <!-- text -->
                                            </Grid.ColumnDefinitions>

                                            <!-- dot -->
                                            <Grid Grid.Column="0">
                                                <Border Background="{StaticResource JobStatusOverdue}" Height="10" Width="10" CornerRadius="20" />
                                            </Grid>

                                            <!-- text -->
                                            <Grid Grid.Column="2">
                                                <TextBlock VerticalAlignment="Center" HorizontalAlignment="Center" Text="Overdue" FontFamily="pack://Traker:,,,/Resources/Fonts/#Inter" FontWeight="Regular" FontSize="14">
                                                    <TextBlock.Style>
                                                        <Style TargetType="TextBlock">
                                                            <Setter Property="Foreground" Value="#D0D0D6"/>
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding IsMouseOver, RelativeSource={RelativeSource AncestorType=Border}}" Value="True">
                                                                    <Setter Property="Foreground" Value="White"/>
                                                                </DataTrigger>
                                                                <DataTrigger Binding="{Binding OptionsStatus[0]}" Value="True">
                                                                    <Setter Property="Foreground" Value="White" />
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </TextBlock.Style>
                                                </TextBlock>
                                            </Grid>
                                        </Grid>
                                    </StackPanel>
                                </Border>
                            </Grid>

                            <!-- paid -->
                            <Grid Grid.Column="2" Grid.Row="2">
                                <Border Cursor="Hand" Opacity="{Binding OpacityStatus[5]}" cal:Message.Attach="[Event MouseLeftButtonUp] = [Action FilterStatusOption(5)]" CornerRadius="15" Height="30">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Setter Property="BorderThickness" Value="1.5"/>
                                            <Setter Property="Background" Value="#2A2A2F"/>
                                            <Setter Property="BorderBrush" Value="Transparent"/>
                                            <Style.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Background" Value="#34343A"/>
                                                    <Setter Property="BorderBrush" Value="{StaticResource AccentColour}"/>
                                                </Trigger>

                                                <DataTrigger Binding="{Binding OptionsStatus[5]}" Value="True">
                                                    <Setter Property="BorderBrush" Value="{StaticResource AccentColour}" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <Border.Effect>
                                        <DropShadowEffect BlurRadius="20" Opacity="0.5" ShadowDepth="8" Direction="270"/>
                                    </Border.Effect>
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="5,3,0,0">
                                        <Grid Width="90">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="auto" />
                                                <!-- dot -->
                                                <ColumnDefinition Width="*" />
                                                <!-- text -->
                                            </Grid.ColumnDefinitions>

                                            <!-- dot -->
                                            <Grid Grid.Column="0">
                                                <Border Background="{StaticResource JobStatusPaid}" Height="10" Width="10" CornerRadius="20" />
                                            </Grid>

                                            <!-- text -->
                                            <Grid Grid.Column="2">
                                                <TextBlock VerticalAlignment="Center" HorizontalAlignment="Center" Text="Paid" FontFamily="pack://Traker:,,,/Resources/Fonts/#Inter" FontWeight="Regular" FontSize="14">
                                                    <TextBlock.Style>
                                                        <Style TargetType="TextBlock">
                                                            <Setter Property="Foreground" Value="#D0D0D6"/>
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding IsMouseOver, RelativeSource={RelativeSource AncestorType=Border}}" Value="True">
                                                                    <Setter Property="Foreground" Value="White"/>
                                                                </DataTrigger>
                                                                <DataTrigger Binding="{Binding OptionsStatus[0]}" Value="True">
                                                                    <Setter Property="Foreground" Value="White" />
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </TextBlock.Style>
                                                </TextBlock>
                                            </Grid>
                                        </Grid>
                                    </StackPanel>
                                </Border>
                            </Grid>
                        </Grid>
                    </Border>
                </Grid>

                <!-- client type -->
                <Grid Grid.Row="1">
                    <Border Background="{StaticResource SortFilterMainInnerBackground}" CornerRadius="11" BorderThickness="1" VerticalAlignment="Center">
                        <Border.BorderBrush>
                            <LinearGradientBrush StartPoint="0.5,0" EndPoint="0.5,1" Opacity="0.12">
                                <GradientStop Color="#26FFFFFF" Offset="0" />
                                <GradientStop Color="Transparent" Offset="1" />
                            </LinearGradientBrush>
                        </Border.BorderBrush>

                        <Grid Margin="10,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />                                <!-- individual -->
                                <ColumnDefinition Width="0.1*" />                                <!-- spacer -->
                                <ColumnDefinition Width="*" />                                <!-- company-->
                            </Grid.ColumnDefinitions>

                            <!-- individual -->
                            <Grid Grid.Column="0" Height="50">
                                <Border Cursor="Hand" Opacity="{Binding OpacityClientType[0]}" cal:Message.Attach="[Event MouseLeftButtonUp] = [Action FilterClientType(0)]" CornerRadius="17" Height="35">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Setter Property="BorderThickness" Value="1.5"/>
                                            <Setter Property="Background" Value="#2A2A2F"/>
                                            <Setter Property="BorderBrush" Value="Transparent"/>
                                            <Style.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Background" Value="#34343A"/>
                                                    <Setter Property="BorderBrush" Value="{StaticResource AccentColour}"/>
                                                </Trigger>

                                                <DataTrigger Binding="{Binding ClientType[0]}" Value="True">
                                                    <Setter Property="BorderBrush" Value="{StaticResource AccentColour}" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <Border.Effect>
                                        <DropShadowEffect BlurRadius="20" Opacity="0.5" ShadowDepth="8" Direction="270"/>
                                    </Border.Effect>
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,3,0,0">
                                        <svg:SvgViewbox                         
                                        RenderOptions.BitmapScalingMode="HighQuality"
                                        SnapsToDevicePixels="True"
                                        Source="/Resources/Media/Images/Icons/Lucide/user-round.svg" Width="15" Height="15"/>
                                        <TextBlock VerticalAlignment="Center" Margin="10,0,0,0" Text="Individual" FontFamily="pack://Traker:,,,/Resources/Fonts/#Inter" FontWeight="Regular" FontSize="14">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Foreground" Value="#D0D0D6"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsMouseOver, RelativeSource={RelativeSource AncestorType=Border}}" Value="True">
                                                            <Setter Property="Foreground" Value="White"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding ClientType[0]}" Value="True">
                                                            <Setter Property="Foreground" Value="White" />
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </StackPanel>
                                </Border>
                            </Grid>

                            <!-- spacer -->
                            <Grid Grid.Column="1" />

                            <!-- company -->
                            <Grid Grid.Column="2" Height="50">
                                <Border Cursor="Hand" Opacity="{Binding OpacityClientType[1]}" cal:Message.Attach="[Event MouseLeftButtonUp] = [Action FilterClientType(1)]" CornerRadius="17" Height="35">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Setter Property="BorderThickness" Value="1.5"/>
                                            <Setter Property="Background" Value="#2A2A2F"/>
                                            <Setter Property="BorderBrush" Value="Transparent"/>
                                            <Style.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Background" Value="#34343A"/>
                                                    <Setter Property="BorderBrush" Value="{StaticResource AccentColour}"/>
                                                </Trigger>

                                                <DataTrigger Binding="{Binding ClientType[1]}" Value="True">
                                                    <Setter Property="BorderBrush" Value="{StaticResource AccentColour}" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <Border.Effect>
                                        <DropShadowEffect BlurRadius="20" Opacity="0.5" ShadowDepth="8" Direction="270"/>
                                    </Border.Effect>
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,3,0,0">
                                        <svg:SvgViewbox                         
                                        RenderOptions.BitmapScalingMode="HighQuality"
                                        SnapsToDevicePixels="True"
                                        Source="/Resources/Media/Images/Icons/Lucide/building.svg" Width="15" Height="15"/>
                                        <TextBlock VerticalAlignment="Center" Margin="10,0,0,0" Text="Comapny" FontFamily="pack://Traker:,,,/Resources/Fonts/#Inter" FontWeight="Regular" FontSize="14">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Foreground" Value="#D0D0D6"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsMouseOver, RelativeSource={RelativeSource AncestorType=Border}}" Value="True">
                                                            <Setter Property="Foreground" Value="White"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding ClientType[1]}" Value="True">
                                                            <Setter Property="Foreground" Value="White" />
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </StackPanel>
                                </Border>
                            </Grid>
                        </Grid>
                    </Border>
                </Grid>
            </Grid>
        </Grid>
    </Border>
</UserControl>
