<UserControl x:Class="Traker.Views.Edit.EditInvoiceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Traker.Views"
             xmlns:cal="http://caliburnmicro.com"
             xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
             xmlns:svg="http://sharpvectors.codeplex.com/svgc/"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Border Background="{StaticResource BorderBrush}" CornerRadius="20" BorderThickness="5" BorderBrush="{StaticResource LightBorderBrush}">
        <Grid Margin="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="0.12*" />                <!-- top bar -->
                <RowDefinition Height="0.12*" />                <!-- spacer -->
                <RowDefinition Height="*" />                <!-- main content -->
                <RowDefinition Height="0.5*" />                <!-- buttons -->
            </Grid.RowDefinitions>

            <!-- top bar -->
            <Grid Grid.Row="0" Margin="30,20,30,0" Height="50">
                <TextBlock Margin="5,0,0,0" VerticalAlignment="Center" HorizontalAlignment="Left" Text="| Invoice " Opacity="0.7" FontFamily="pack://Traker:,,,/Resources/Fonts/#Montserrat" FontWeight="Bold" Foreground="{StaticResource FontPrimaryBrush}" FontSize="58" />
                <Border cal:Message.Attach="[Event MouseLeftButtonUp] = [Action Exit()]" Background="Transparent" HorizontalAlignment="Right" VerticalAlignment="Top" CornerRadius="20" Height="20" Width="20">
                    <svg:SvgViewbox                         
                RenderOptions.BitmapScalingMode="HighQuality"
                SnapsToDevicePixels="True"
                Source="/Resources/Media/Images/Icons/Lucide/x.svg" Width="15" Height="15"/>
                </Border>
            </Grid>

            <!-- spacer -->
            <Grid Grid.Row="1" />

            <!-- main content -->
            <Grid Grid.Row="2" Height="620" Width="1000">
                <Border Background="#494949" CornerRadius="20" Margin="50,50" Padding="30">
                    <wv2:WebView2 x:Name="PdfViewer"/>
                </Border>
            </Grid>

            <!-- buttons -->
            <Grid Grid.Row="4">
                <Border Background="#181818" CornerRadius="0,0,20,20" Padding="30, 0">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>                            <!-- delete -->
                            <ColumnDefinition Width="*"/>                            <!-- action button -->
                            <ColumnDefinition Width="*"/>                            <!-- download -->
                        </Grid.ColumnDefinitions>

                        <!-- delete -->
                        <Grid Grid.Column="0">
                            <Border CornerRadius="15" HorizontalAlignment="Center" VerticalAlignment="Center">
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Setter Property="Background" Value="Transparent"/>
                                        <Style.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Background" Value="#303030"/>
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <StackPanel Margin="5" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                                    <Border Background="#1E1E1E"  HorizontalAlignment="Center" VerticalAlignment="Center" CornerRadius="40" Height="50" Width="50">
                                        <svg:SvgViewbox                         
                                        RenderOptions.BitmapScalingMode="HighQuality"
                                        SnapsToDevicePixels="True"
                                        Source="/Resources/Media/Images/Icons/Lucide/trash-2.svg" Width="25" Height="25"/>
                                    </Border>
                                    <TextBlock VerticalAlignment="Center" Margin="10,0,0,0" Text="Delete" FontFamily="pack://Traker:,,,/Resources/Fonts/#Montserrat" FontWeight="Regular" Foreground="{StaticResource FontSecondaryBrush}" FontSize="22" />
                                </StackPanel>
                            </Border>
                        </Grid>

                        <!-- mark as paid -->
                        <Grid Grid.Column="1">
                            <Border cal:Message.Attach="[Event MouseLeftButtonUp] = [Action ConfirmJobChanges()]" Background="Green" CornerRadius="25" Height="45" Width="200" Margin="0,0,0,0" BorderThickness="2" BorderBrush="Green">
                                <TextBlock VerticalAlignment="Center" HorizontalAlignment="Center" Margin="10,0" Text="PAID" FontFamily="pack://Traker:,,,/Resources/Fonts/#Public Sans" FontWeight="Bold" Foreground="{StaticResource FontPrimaryBrush}" FontSize="18" />
                            </Border>
                        </Grid>
                        
                        <!-- download -->
                        <Grid Grid.Column="2">
                            <Border CornerRadius="15" HorizontalAlignment="Center" VerticalAlignment="Center">
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Setter Property="Background" Value="Transparent"/>
                                        <Style.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Background" Value="#303030"/>
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <StackPanel Margin="5" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                                    <Border Background="#1E1E1E"  HorizontalAlignment="Center" VerticalAlignment="Center" CornerRadius="40" Height="50" Width="50">
                                        <svg:SvgViewbox                         
                                            RenderOptions.BitmapScalingMode="HighQuality"
                                            SnapsToDevicePixels="True"
                                            Source="/Resources/Media/Images/Icons/Lucide/download.svg" Width="25" Height="25"/>
                                    </Border>
                                    <TextBlock VerticalAlignment="Center" Margin="10,0,0,0" Text="Download" FontFamily="pack://Traker:,,,/Resources/Fonts/#Montserrat" FontWeight="Regular" Foreground="{StaticResource FontSecondaryBrush}" FontSize="22" />
                                </StackPanel>                                
                            </Border>
                        </Grid>
                    </Grid>
                </Border>
            </Grid>
        </Grid>
    </Border>
</UserControl>
