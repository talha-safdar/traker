<UserControl x:Class="Traker.Views.EditClientView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Traker.Views"
             xmlns:cal="http://caliburnmicro.com"
             xmlns:svg="http://sharpvectors.codeplex.com/svgc/"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Border Background="{StaticResource BorderBrush}" CornerRadius="20" BorderThickness="5" BorderBrush="{StaticResource LightBorderBrush}">
        <Grid Margin="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="0.05*" /> <!-- top bar -->
                <RowDefinition Height="0.1*" /> <!-- client name -->
                <RowDefinition Height="0.1*" /> <!-- created date -->
                <RowDefinition Height="*" /> <!-- main content -->
                <RowDefinition Height="0.2*" /> <!-- buttons -->
            </Grid.RowDefinitions>

            <!-- top bar -->
            <Grid Grid.Row="0" Margin="0,20,30,0">
                <Border cal:Message.Attach="[Event MouseLeftButtonUp] = [Action Exit()]" Background="Transparent"  HorizontalAlignment="Right" VerticalAlignment="Top" CornerRadius="20" Height="20" Width="20">
                    <svg:SvgViewbox                         
                        RenderOptions.BitmapScalingMode="HighQuality"
                        SnapsToDevicePixels="True"
                        Source="/Resources/Media/Images/Icons/Lucide/x.svg" Width="15" Height="15"/>
                </Border>
            </Grid>

            <!-- client name -->
            <Grid Grid.Row="1" Margin="30,0,0,0">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Margin="5,0,0,0" Text="{Binding ClientName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" FontFamily="pack://Traker:,,,/Resources/Fonts/#Montserrat" FontWeight="Regular" Foreground="{StaticResource FontPrimaryBrush}" FontSize="34" />
                    <Border Background="Red" Height="20" Width="20" cal:Message.Attach="[Event MouseLeftButtonUp] = [Action OpenJobsList()]" />
                </StackPanel>
            </Grid>
            
            <!-- created date -->
            <Grid Grid.Row="2" Margin="30,0,0,0">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Margin="5,0,0,0" Text="Created Date - " FontFamily="pack://Traker:,,,/Resources/Fonts/#Montserrat" FontWeight="Regular" Foreground="{StaticResource FontPrimaryBrush}" FontSize="22" Opacity="0.8" />                    
                    <TextBlock Margin="0,0,0,0" Text="{Binding CreatedDate, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" FontFamily="pack://Traker:,,,/Resources/Fonts/#Montserrat" FontWeight="Regular" Foreground="{StaticResource FontPrimaryBrush}" FontSize="22" Opacity="0.8" />                    
                </StackPanel>
            </Grid>

            <!-- main content -->
            <Grid Grid.Row="3" Margin="30,0,30,30">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" /> <!-- who are they -->
                    <ColumnDefinition Width="0.005*" /> <!-- spacer -->
                    <ColumnDefinition Width="*" /> <!-- where are they-->
                </Grid.ColumnDefinitions>

                <Grid.RowDefinitions>
                    <RowDefinition Height="0.2*" /> <!-- type -->
                    <RowDefinition Height="0.05*" /> <!-- spacer -->
                    <RowDefinition Height="*" /> <!-- textboxes -->
                </Grid.RowDefinitions>
                
                <!-- type -->
                <Grid Grid.Row="0" Grid.ColumnSpan="3">
                    <!-- type -->
                    <StackPanel Orientation="Horizontal" Margin="10" HorizontalAlignment="Center">
                        <TextBlock VerticalAlignment="Center" Margin="0,0,10,0" Text="Client Type" FontFamily="pack://Traker:,,,/Resources/Fonts/#Montserrat" FontWeight="Regular" Foreground="{StaticResource FontPrimaryBrush}" FontSize="22" />
                        <ComboBox SelectedValue="{Binding ClientType, Mode=TwoWay}" SelectedValuePath="Content" Width="150" FontSize="22" Background="Black">
                            <ComboBoxItem Content="Individual" FontSize="22" FontFamily="pack://Traker:,,,/Resources/Fonts/#Public Sans" />
                            <ComboBoxItem Content="Company" FontSize="22" FontFamily="pack://Traker:,,,/Resources/Fonts/#Public Sans" />
                        </ComboBox>
                    </StackPanel>
                </Grid>

                <!-- who are they-->
                <Grid Grid.Column="0" Grid.Row="2" Margin="0,0,20,0">
                    <StackPanel Orientation="Vertical">
                        <!--company name (greyd out if solo)-->
                        <StackPanel Orientation="Vertical" Margin="10">
                            <TextBlock Margin="5,0,0,0" Text="Company Name" FontFamily="pack://Traker:,,,/Resources/Fonts/#Montserrat" FontWeight="Regular" Foreground="{StaticResource FontPrimaryBrush}" FontSize="22" />
                            <TextBox Text="{Binding CompanyName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" MaxLength="35" AcceptsReturn="False" TextWrapping="Wrap" Padding="10" FontSize="22" FontFamily="pack://Traker:,,,/Resources/Fonts/#Public Sans" />
                        </StackPanel>
                        
                        <!--client name-->
                        <StackPanel Orientation="Vertical" Margin="10">
                            <TextBlock Margin="5,0,0,0" Text="Full Name" FontFamily="pack://Traker:,,,/Resources/Fonts/#Montserrat" FontWeight="Regular" Foreground="{StaticResource FontPrimaryBrush}" FontSize="22" />
                            <TextBox Text="{Binding ClientName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" MaxLength="35" AcceptsReturn="False" TextWrapping="Wrap" Padding="10" FontSize="22" FontFamily="pack://Traker:,,,/Resources/Fonts/#Public Sans" />
                        </StackPanel>

                        <!--email-->
                        <StackPanel Orientation="Vertical" Margin="10">
                            <TextBlock Margin="5,0,0,0" Text="Email Address" FontFamily="pack://Traker:,,,/Resources/Fonts/#Montserrat" FontWeight="Regular" Foreground="{StaticResource FontPrimaryBrush}" FontSize="22" />
                            <TextBox Text="{Binding ClientEmail, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" MaxLength="35" AcceptsReturn="False" TextWrapping="Wrap" Padding="10" FontSize="22" FontFamily="pack://Traker:,,,/Resources/Fonts/#Public Sans" />
                        </StackPanel>

                        <!--phone number-->
                        <StackPanel Orientation="Vertical" Margin="10">
                            <TextBlock Margin="5,0,0,0" Text="Phone Number" FontFamily="pack://Traker:,,,/Resources/Fonts/#Montserrat" FontWeight="Regular" Foreground="{StaticResource FontPrimaryBrush}" FontSize="22" />
                            <TextBox Text="{Binding PhoneNumber, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" MaxLength="35" AcceptsReturn="False" TextWrapping="Wrap" Padding="10" FontSize="22" FontFamily="pack://Traker:,,,/Resources/Fonts/#Public Sans" />
                        </StackPanel>
                    </StackPanel>
                </Grid>
                
                <!-- spacer -->
                <Grid Grid.Column="1" Grid.Row="2">
                    <Border Background="DimGray" CornerRadius="2">
                        
                    </Border>
                </Grid>
                
                <!-- where are they -->
                <Grid Grid.Column="2" Grid.Row="2" Margin="20,0,0,0">
                    <StackPanel Orientation="Vertical">
                        <!--Home address-->
                        <StackPanel Orientation="Vertical" Margin="10">
                            <TextBlock Margin="5,0,0,0" Text="Home Address" FontFamily="pack://Traker:,,,/Resources/Fonts/#Montserrat" FontWeight="Regular" Foreground="{StaticResource FontPrimaryBrush}" FontSize="22" />
                            <TextBox Text="{Binding BillingAddress, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" MaxLength="35" AcceptsReturn="False" TextWrapping="Wrap" Padding="10" FontSize="22" FontFamily="pack://Traker:,,,/Resources/Fonts/#Public Sans" />
                        </StackPanel>
                        
                        <!--city-->
                        <StackPanel Orientation="Vertical" Margin="10">
                            <TextBlock Margin="5,0,0,0" Text="City" FontFamily="pack://Traker:,,,/Resources/Fonts/#Montserrat" FontWeight="Regular" Foreground="{StaticResource FontPrimaryBrush}" FontSize="22" />
                            <TextBox Text="{Binding City, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" MaxLength="35" AcceptsReturn="False" TextWrapping="Wrap" Padding="10" FontSize="22" FontFamily="pack://Traker:,,,/Resources/Fonts/#Public Sans" />
                        </StackPanel>

                        <!--postcode-->
                        <StackPanel Orientation="Vertical" Margin="10">
                            <TextBlock Margin="5,0,0,0" Text="Postcode" FontFamily="pack://Traker:,,,/Resources/Fonts/#Montserrat" FontWeight="Regular" Foreground="{StaticResource FontPrimaryBrush}" FontSize="22" />
                            <TextBox Text="{Binding Postcode, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" MaxLength="35" AcceptsReturn="False" TextWrapping="Wrap" Padding="10" FontSize="22" FontFamily="pack://Traker:,,,/Resources/Fonts/#Public Sans" />
                        </StackPanel>

                        <!--country-->
                        <StackPanel Orientation="Vertical" Margin="10">
                            <TextBlock Margin="5,0,0,0" Text="Country" FontFamily="pack://Traker:,,,/Resources/Fonts/#Montserrat" FontWeight="Regular" Foreground="{StaticResource FontPrimaryBrush}" FontSize="22" />
                            <TextBox Text="{Binding Country, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" MaxLength="35" AcceptsReturn="False" TextWrapping="Wrap" Padding="10" FontSize="22" FontFamily="pack://Traker:,,,/Resources/Fonts/#Public Sans" />
                        </StackPanel>
                    </StackPanel>
                </Grid>       
            </Grid>

            <!-- buttons -->
            <Grid Grid.Row="4">
                <Border Background="#181818" CornerRadius="0,0,20,20">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                        <Border cal:Message.Attach="[Event MouseLeftButtonUp] = [Action CancelEditClient()]" Background="Transparent" CornerRadius="25" Height="45" Width="200" Margin="0,0,30,0" BorderThickness="2" BorderBrush="Red">
                            <TextBlock VerticalAlignment="Center" HorizontalAlignment="Center" Margin="10,0" Text="Cancel" FontFamily="pack://Traker:,,,/Resources/Fonts/#Public Sans" FontWeight="Bold" Foreground="Red" FontSize="16" />
                        </Border>
                        <Border cal:Message.Attach="[Event MouseLeftButtonUp] = [Action ConfirmEditClient()]" Background="Green" CornerRadius="25" Height="45" Width="200" Margin="0,0,0,0" BorderThickness="2" BorderBrush="Green">
                            <TextBlock VerticalAlignment="Center" HorizontalAlignment="Center" Margin="10,0" Text="Confirm" FontFamily="pack://Traker:,,,/Resources/Fonts/#Public Sans" FontWeight="Bold" Foreground="{StaticResource FontPrimaryBrush}" FontSize="16" />
                        </Border>
                    </StackPanel>
                </Border>
            </Grid>
        </Grid>
    </Border>
</UserControl>